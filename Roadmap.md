# LearninRust Map

## Starting Out

### Setting Up Environment

* Install [VS Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)
* Nvm I Already Had That Lol
* Install Rust From [rustup.rs](https://rustup.rs)
* Check ```rustc --version``` and ```cargo --version```
* Yooo It Works!
* Use ```cargo init```

### Simple Stuff

* Simple Code To Print Hello Cruel World
* Build Using ```rustc ./src/main.rs```
* Works!
* The ```todo!``` macro
* The ```println!``` macro with args
* Learnt about `{}`. Kinda like \` \` in JS.

### Variables Starter

* ```let``` is variable thing, as always.
* Mutable and Immutable are different
* Something called `borrow checking` exists, learn more later
* Data Types, u8, s8, u16 etc.
* Apparently String and str and &str are different things, learn more later.
* Tuples in rust, `let tuple_e = ('E', 5i32, true);`

### Structs

* Basically Classes from JS.
* Three types, Classic, Tuple and Unit.
* Classic is classic (lol), Tuple is `()` and Unit will learn later.
* Can set data types in classic and tuples
* Instantiate is just specifying values.
* Use with x.0 for tuples and x.y for classics.
* `String::from("Some String Here")` to convert from `&str` to `String`.
* Enums In Rust
* Built A Simple Car Making Program.